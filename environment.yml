name: chimera

channels:
  - defaults

dependencies:
  - biopython
  - Flask>=1.0
  - Jinja2>=2.10.1
  - MarkupSafe
  - matplotlib
  - pandas
  - pip
  - pytest
  - python=3.7
  - tqdm
  - pip:
    - coveralls
    - prody --install-option="--no-binary prody"
    - pytest-cov
    - weblogo
